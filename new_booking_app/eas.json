{
  "cli": {
    "version": ">= 15.0.6",
    "appVersionSource": "remote"
  },
  "build": {
    "preview": {
      "distribution": "internal",
      "env": {
        "API_BASEURL": "${API_BASEURL}"
      }
    },
    "production": {
      "distribution": "store",
      "env": {
        "API_BASEURL": "${API_BASEURL}"
      }
    }
  },
  "submit": {
    "production": {}
  }
}
